[package]
name = "dataforge_ffi_spike"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["staticlib", "rlib"]

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"
debug = 1
